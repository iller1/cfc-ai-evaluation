# CASE_02 — Unresolved current state / definite negative conclusion

Fixture revision **0.2**. This supersedes CASE_02 v0.1.

The model conclusion is `DemoSubject is not safe.` A proposition-relevant trusted record exists but is not yet epistemically available at the evaluation date. The frozen controller therefore preserves `UNRESOLVED` rather than treating lack of current positive support as proof of the negative claim.

The required result is a **full audit-shaped** public API result, not an early integration/context stop.

Synthetic case-scoped host attestations/verifiers are demonstration infrastructure only. No production IAM/crypto/external-validation claim is made.

Production exclusions: no `cfc_anchor._engine`, no `cfc_anchor.testing`, no private `Controller` method, and no reimplementation of closure rules.
